{
  "name": "rest",
  "version": "1.0.0",
  "description": "Rest API for iPeeFreely",
  "main": "db.json",
  "scripts": {
    "test": "json-server --watch db.json",
    "start": "json-server --watch db.json",
    "postinstall": "json-server --watch db.json --port 80"

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.8.21"
  }
}
